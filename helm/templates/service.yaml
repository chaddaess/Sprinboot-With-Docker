apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-springboot-app.fullname" . }}
  labels:
    {{- include "my-springboot-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-springboot-app.selectorLabels" . | nindent 4 }}
